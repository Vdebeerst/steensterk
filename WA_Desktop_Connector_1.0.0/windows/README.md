# Windows connector

The seven folders under `src` are internal components. They are packaged into one GUI executable and one installer.

Build with `build_installer.ps1` on Windows. The resulting customer artifact is:

`dist/WA_Desktop_Connector_Setup_0.2.4.exe`
